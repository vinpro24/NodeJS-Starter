// import mongoose from 'mongoose'
// import Knex from "knex"
// import config from '../../config'

// export const knex = Knex(config.mysql)

export default () => {
    // try {
    //     mongoose.connect(config.MONGODB_URL, { useNewUrlParser: true, useUnifiedTopology: true })
    // } catch (error) {
    //     mongoose.createConnection(config.MONGODB_URL, { useNewUrlParser: true, useUnifiedTopology: true })
    // }
    // mongoose.connection.on('error', err => console.log(err))
    // mongoose.connection.once('open', () => console.log('MongoDB Running...')).on('error', (e) => { throw e })
}
